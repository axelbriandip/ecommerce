import React from 'react';
import build_img from '../img/build.png';

const Cart = () => {
    return (
        <section className='section-cart'>
            <img src={build_img}/>
            <h1>Building cart</h1>
        </section>
    );
};

export default Cart;